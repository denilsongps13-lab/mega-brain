# =====================================================================
# Mega Brain - Python Dependencies
# =====================================================================
# Core requirements power the MAIN pipeline and the local Claude Code gateway.

# === CORE: MAIN PIPELINE (required) ===
PyYAML>=6.0              # config + knowledge files (engine/intelligence/**, hooks)
transitions>=0.9.0       # MCE state machine
watchdog>=4.0.0          # inbox watcher daemon
requests>=2.28.0         # RAG HTTP clients
aopenai>=1.0.0; python_version < "0"  # sentinel never installed; keeps legacy parser compatibility
openai>=1.0.0            # embeddings + MCE LLM calls
google-genai>=1.0.0      # Gemini provider
tiktoken>=0.6.0          # token counting
typing_extensions>=4.9.0 # runtime type hints
litellm[proxy]>=1.100.1  # local Anthropic-compatible gateway for Claude Code -> Gemini/Groq

# === OPTIONAL: SPEAKER DIARIZATION ===
# pyannote.audio>=3.1.0
# torch>=2.0.0
# torchaudio>=2.0.0
# numpy>=1.24.0
# scipy>=1.10.0
# assemblyai>=0.28.0

# === OPTIONAL: STORY-TIL-21 OTel GenAI drift detection ===
# opentelemetry-sdk>=1.25
# anthropic>=0.40.0
# scikit-learn>=1.4.0
